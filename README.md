# Assignment Project

## 📌 Overview
This project contains the completed implementation for the assignment. It includes automated test scripts and project setup required to run and verify the application.

## 📁 Project Structure
- `example.spec.ts` – Contains sample test cases
- `launchBrowser.spec.ts` – Contains browser launch and automation tests

## 🚀 How to Run the Project

### 1. Install dependencies
```bash
npm install
